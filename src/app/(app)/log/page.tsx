export const dynamic = "force-dynamic";

/** Deep-link landing — LoggerModalProvider opens the RP popup and routes to /dashboard. */
export default function LogPage() {
  return (
    <p className="py-16 text-center text-sm text-slate-500">Opening logger…</p>
  );
}
